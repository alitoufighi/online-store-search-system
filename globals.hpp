#ifndef _GLOBALS_H_
#define _GLOBALS_H_

#include <string>

// we need to declare them globally, so that we can use them in compare() for sorting
extern std::string sort_type;
extern int sort_field_index, num_of_fields;

#endif
